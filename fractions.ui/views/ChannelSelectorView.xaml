<UserControl x:Class="fractions.ui.views.ChannelSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:fractions.ui.viewmodels"
             mc:Ignorable="d" d:DesignHeight="28" d:DesignWidth="100"
             d:DataContext="{d:DesignInstance Type=vm:ChannelViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="C" ToolTip="Channel"/>
        <ComboBox x:Name="ChannelComboBox" Grid.Column="1"
                  ItemsSource="{Binding Channels}"
                  SelectedItem="{Binding Channel, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName" />
    </Grid>
</UserControl>