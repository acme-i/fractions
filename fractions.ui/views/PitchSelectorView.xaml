<UserControl x:Class="fractions.ui.views.PitchSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:fractions.ui.viewmodels"
             mc:Ignorable="d" d:DesignHeight="28" d:DesignWidth="100"
             d:DataContext="{d:DesignInstance Type=vm:PitchViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="P" ToolTip="Pitch" Margin="4" />
        <ComboBox x:Name="PitchComboBox" Grid.Column="1"
                  ItemsSource="{Binding AllPitches}"
                  SelectedItem="{Binding Pitch, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName" />
    </Grid>
</UserControl>