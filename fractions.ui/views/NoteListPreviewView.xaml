<UserControl x:Class="fractions.ui.views.NoteListPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:fractions.ui.views"
             xmlns:vm="clr-namespace:fractions.ui.viewmodels"
             mc:Ignorable="d"
             d:DesignHeight="160"
             d:DesignWidth="520"
             d:DataContext="{d:DesignInstance Type=vm:NoteOnOffListViewModel}">
    <DataGrid ItemsSource="{Binding Source}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              IsReadOnly="True"
              SelectionMode="Single"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              BorderThickness="0"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              EnableRowVirtualization="True"
              EnableColumnVirtualization="True">

        <DataGrid.Columns>
            <DataGridTextColumn Header="Ch" Binding="{Binding Channel}" Width="30"/>
            <DataGridTextColumn Header="Pitch" Binding="{Binding Pitch}" Width="50"/>
            <DataGridTextColumn Header="Vel" Binding="{Binding Velocity}" Width="40"/>
            <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat=N2}" Width="60"/>
            <DataGridTextColumn Header="Dur" Binding="{Binding Duration, StringFormat=N2}" Width="60"/>
            <DataGridTextColumn Header="Pan" Binding="{Binding Pan}" Width="40"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>