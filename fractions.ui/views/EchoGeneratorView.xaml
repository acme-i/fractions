<UserControl x:Class="fractions.ui.views.EchoGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:fractions.ui.viewmodels"
             mc:Ignorable="d"
             d:DesignHeight="60"
             d:DesignWidth="460"
             d:DataContext="{d:DesignInstance Type=vm:EchoGeneratorViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Echoes" VerticalAlignment="Center" Margin="0,0,4,0"/>
        <TextBox Grid.Column="1" Text="{Binding EchoCount}" VerticalAlignment="Center"/>

        <TextBlock Grid.Column="2" Text="Time" VerticalAlignment="Center" Margin="8,0,4,0"/>
        <TextBox Grid.Column="3" Text="{Binding TimeOffset}" VerticalAlignment="Center"/>

        <TextBlock Grid.Column="4" Text="Pitch" VerticalAlignment="Center" Margin="8,0,4,0"/>
        <TextBox Grid.Column="5" Text="{Binding PitchOffset}" VerticalAlignment="Center"/>

        <TextBlock Grid.Column="6" Text="Vel" VerticalAlignment="Center" Margin="8,0,4,0"/>
        <TextBox Grid.Column="7" Text="{Binding VelocityScale}" VerticalAlignment="Center"/>
    </Grid>
</UserControl>