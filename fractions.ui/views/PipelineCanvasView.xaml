<UserControl x:Class="fractions.ui.views.PipelineCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:fractions.ui.viewmodels"
             xmlns:views="clr-namespace:fractions.ui.views"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="520"
             d:DataContext="{d:DesignInstance Type=vm:PipelineViewModel}">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:MidiFileSourceViewModel}">
            <views:MidiFileSourceView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EchoGeneratorViewModel}">
            <views:EchoGeneratorView/>
        </DataTemplate>
    </UserControl.Resources>

    <StackPanel Orientation="Vertical" Margin="4">
        <ContentControl Content="{Binding Source}" />

        <StackPanel Orientation="Horizontal" Margin="0,6,0,4">
            <Button Content="+ Echo" Command="{Binding AddEchoCommand}" Margin="0,0,6,0"/>
            <Button Content="Run ▶" Command="{Binding RunPipelineCommand}"/>
        </StackPanel>

        <ItemsControl ItemsSource="{Binding Processors}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="LightGray" Padding="4" Margin="0,0,0,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ContentControl Content="{Binding}"/>

                            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="6,0,0,0">
                                <Button Content="▲"
                                        Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="▼"
                                        Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <views:NoteListPreviewView DataContext="{Binding}" />
    </StackPanel>
</UserControl>